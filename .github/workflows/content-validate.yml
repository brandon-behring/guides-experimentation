name: content-validate

on:
  push:
    branches: [main]
    paths:
      - 'src/content/**'
      - 'src/content.config.ts'
      - '.github/workflows/content-validate.yml'
  pull_request:
    paths:
      - 'src/content/**'
      - 'src/content.config.ts'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: book-scaffold validate
        run: npx book-scaffold validate --preset research-portfolio
      - name: Astro content schema check (v0.2 extension)
        run: npx astro sync
        env:
          BOOK_PRESET: research-portfolio
